export const grid = (prevState = [], action = {}) => {
  console.log('get board');

  return prevState;
};

export const neighbours = (prevState = [], action = {}) => {
  console.log('getNeighbours');

  return prevState;
};

